<?xml version="1.0" encoding="utf-8" ?>
    <ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:local="clr-namespace:SampleApp"
                 x:Class="SampleApp.MainPage">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackLayout Grid.Column="0">
                <Button Text="Toggle" Command="{Binding Toggle}" />
            </StackLayout>

            <local:PanelAView Grid.Column="1"
                              BindingContext="{Binding PanelAViewModel}"
                              PanelVisible="{Binding IsVisible}" />
            <local:PanelBView Grid.Column="1"
                              BindingContext="{Binding PanelBViewModel}"
                              PanelVisible="{Binding IsVisible}" />
        </Grid>

    </ContentPage>
